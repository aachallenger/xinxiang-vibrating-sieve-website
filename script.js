const SITE_EMAIL = 'info@xinxiangvibratingsieve.com';
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button))});let count=0;document.querySelectorAll('.product-card').forEach(card=>{card.hidden=button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter;if(!card.hidden)count++});document.querySelector('#product-status').textContent=`${count} equipment options shown.`}));
const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('open')}));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.focus()}});
document.querySelectorAll('[data-email]').forEach(a=>{a.href=`mailto:${SITE_EMAIL}`;a.textContent=SITE_EMAIL});document.querySelector('#year').textContent=new Date().getFullYear();
let lastEnquiry='';document.querySelector('#inquiry-form')?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);lastEnquiry=`Hello Xinxiang Vibrating Sieve,\n\nName: ${data.get('name')}\nEmail: ${data.get('email')}\nCompany: ${data.get('company')}\nEquipment: ${data.get('equipment')}\n\nRequirements:\n${data.get('requirements')}`;const url=`mailto:${SITE_EMAIL}?subject=${encodeURIComponent('Equipment enquiry — '+data.get('equipment'))}&body=${encodeURIComponent(lastEnquiry)}`;document.querySelector('#form-status').textContent='Your enquiry is prepared, not sent. Send it from your email application. If it does not open, copy the text and email us directly.';document.querySelector('#copy-inquiry').hidden=false;window.location.href=url});document.querySelector('#copy-inquiry')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(lastEnquiry);document.querySelector('#form-status').textContent='Enquiry copied. Paste it into an email and send it to '+SITE_EMAIL;}catch{document.querySelector('#form-status').textContent='Copy is unavailable in this browser. Please select and copy the enquiry below.';let area=document.querySelector('#copy-fallback');if(!area){area=document.createElement('textarea');area.id='copy-fallback';area.readOnly=true;area.setAttribute('aria-label','Prepared enquiry text');document.querySelector('#inquiry-form').append(area)}area.value=lastEnquiry;area.focus();area.select()}});

const params=new URLSearchParams(location.search);const equipmentField=document.querySelector("#equipment");if(equipmentField&&params.has("equipment")){const value=params.get("equipment");if([...equipmentField.options].some(o=>o.value===value))equipmentField.value=value;}const req=document.querySelector("#requirements");if(req&&params.has("subject"))req.value="Project: "+params.get("subject").slice(0,300)+"\nMaterial: \nTarget capacity: \nRequired particle size: ";
